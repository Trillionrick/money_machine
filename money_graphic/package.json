{
  "name": "money_graphic",
  "version": "2.0.0",
  "description": "The Graph Protocol subgraph for Money Machine token tracking",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio money_graphic",
    "deploy:studio": "graph deploy --studio money_graphic",
    "create-local": "graph create --node http://localhost:8020/ money_graphic",
    "remove-local": "graph remove --node http://localhost:8020/ money_graphic",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 money_graphic",
    "test": "graph test",
    "test:coverage": "graph test -- --coverage"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/money_machine"
  },
  "keywords": [
    "the-graph",
    "subgraph",
    "graphql",
    "blockchain",
    "ethereum",
    "defi"
  ]
}
